import { GenderByIdPipe } from './gender-by-id-pipe';

describe('GenderByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
